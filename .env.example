# GitHub Personal Access Token (optional, but recommended to avoid rate limits)
# Create one at: https://github.com/settings/tokens
# Required scopes: public_repo (for public repositories)
GITHUB_TOKEN=your_github_token_here

# GitLab Personal Access Token (optional, for GitLab repositories)
# Create one at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: read_api, read_repository
GITLAB_TOKEN=your_gitlab_token_here
